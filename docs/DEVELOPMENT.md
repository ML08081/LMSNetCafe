# Development Notes

This directory is reserved for implementation notes that are separate from the original `开发文档` reference set.

## First Milestone

- Implement JWT authentication and role authorization in `backend`.
- Build member registration, recharge and account ledger APIs.
- Build device seat map and session start/settlement workflow in `frontend-admin`.
- Replace placeholder face matching in `face-service` with OpenCV based feature extraction.
- Connect `desktop-pet` to backend websocket channel `/ws/client/{deviceCode}`.
