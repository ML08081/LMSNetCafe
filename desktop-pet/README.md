# Desktop Pet

Electron + Vue desktop pet client for customer machines.

```powershell
npm install
npm run dev
```

The initial scaffold focuses on session display and reminder UI. Electron packaging is prepared with `electron-builder`.
